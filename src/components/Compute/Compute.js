import React from 'react';
import './Compute.css'

const Compute = () => {
    return (
        <div className = "compute-cart">
            <p>Friends:</p>
        </div>
    );
};

export default Compute;